import { Routes } from "@angular/router";
import { RequestDrawerComponent } from "./request-drawer.component";

export const route: Routes = [
  {
    path: 'list',
    component: RequestDrawerComponent
  }
]